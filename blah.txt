if [ "$(command -v apt)" ]; then
  distro=ubuntu

    elif [ "$(command -v apt-get)" ]; then
      distro=ubuntu

    elif [ "$(command -v dnf)" ]; then
      distro=fedora

    elif [ "$(command -v yum)" ]; then
      distro=fedora

    elif [ "$(command -v pacman)" ]; then
      distro=arch

    elif [ "$(command -v zypper)" ]; then
      distro=opensuse

    else
      # Possible link to tarballs
