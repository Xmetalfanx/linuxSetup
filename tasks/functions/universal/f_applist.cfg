function checkApp
{
  echo $1
  echo -e ""

  ## note to self: this check below doesn't seem to be working

  if [ "${usefulApp[appNum]}" == 'googleChrome' ]; then
    echo -e "Passing to Google Chrome install function"

    userPrompt

    appNum=appNum + 1
  else

    echo -e "Exception "
  fi

}



function installAppList
{
  echo -e "Testing message: Passing to Check function"
  checkApp ${1}
  userPrompt

    if [ "${distroBase}" == 'ubuntu' ]; then
      sudo apt install $appList

    elif [ "${distroBase}" == 'fedora' ]; then
        sudo dnf install $appList
    elif [ "${distroBase}" == 'opennsuse' ]; then
        sudo zypper install $appList
    elif [ "${distroBase}" == 'arch' ]; then
        sudo pacman -S $appList
    else
       echo "FILLER TEXT FOR NOW ... "
    fi

    userPrompt

}
