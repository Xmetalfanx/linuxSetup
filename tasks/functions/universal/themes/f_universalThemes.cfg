# source: http://www.devpy.me/10-of-the-best-linux-themes-compared/


function installVertex()
{

  #Tumbleweed

  zypper addrepo http://download.opensuse.org/repositories/home:Horst3180/openSUSE_Tumbleweed/home:Horst3180.repo
  zypper refresh
  zypper install vertex-theme

  #Leap 42.1
  #zypper addrepo http://download.opensuse.org/repositories/home:Horst3180/openSUSE_Leap_42.1/home:Horst3180.repo
  #zypper refresh
  #zypper install vertex-theme



  # Ubuntu based
  sudo sh -c "echo 'deb http://download.opensuse.org/repositories/home:/Horst3180/xUbuntu_16.04/ /' >> /etc/apt/sources.list.d/vertex-theme.list"
  sudo apt update
  sudo apt install vertex-theme


}

################################################################################
function installNumixTheme()
{
    #Installation from PPA (Ubuntu)

      sudo add-apt-repository ppa:numix/ppa
      sudo apt update
      sudo apt install numix-gtk-theme

    #Arch Linux

      ## Install from community repo.
      pacman -S numix-themes

    # OpenSuse 
      ## Tumbleweed 
      ### Not 100% sure what repos its coming from 
      ### gtk2-metatheme-numix gtk3-metatheme-numix cinnamon-metatheme-numix numix-icon-theme


}


###########################################################################


function arcInstall()
{

    if [ "$distroBase" == 'ubuntu' ]; then
      
      ## What about the Arc theme ... I know the Noobslab theme ppa has them but is it in the default repo?
        ## if so ... use default repo's version to avoid adding PPAs if not needed
      installApp arc-icons


       sudo sh -c "echo 'deb http://download.opensuse.org/repositories/home:/Horst3180/xUbuntu_16.04/ /' >> /etc/apt/sources.list.d/arc-theme.list"
      sudo apt update
      installApp arc-theme


    elif [ "$distroBase" == 'fedora' ]; then
      installApp gtk-murrine-engine

      ## Other code removed since it appears this is included since at least Fedora 26 in the default Fedora repos .. no need for extra steps 

      # arc-theme is there but I do not see a package for the arc icons listed in the DEFAULT repos 


    elif [ "$distroBase" == 'opensuse' ]; then
      installApp gtk2-engine-murrine

      zypperRefresh

    ## openSUSE
      #Tumbleweed

      zypper addrepo http://download.opensuse.org/repositories/home:Horst3180/openSUSE_Tumbleweed/home:Horst3180.repo
      zypper refresh
      zypper install arc-theme


    elif [ "$distroBase" == 'arch' ]; then
      echo -e "Installing the GTK-Engine-murrine package"
      installApp gtk-engine-murrine

      echo "Adding Arc Theme and Icon Pack"
      installApp arc-icon-theme arc-firefox-theme arc-suite gtk-theme-arc

    elif [ "$distroBase" == 'solus']; then 

      sudo eopkg arc-gtk-theme

    else
      return 
    fi

    # This may not work with Arch based distros 
    installApp arc-theme

}
################################################################################
## Adapta GTK Theme Section 

function openSuseAdaptaAdd()
{
  
  ## TW - @System repo - cinnamon-theme-adapta gtk2-metatheme-adapta gtk3-metatheme-adapta gtk4-metatheme-adapta metacity-theme-adapta plank-theme-adapta xfwm4-theme-adapta gnome-shell-theme-adapta openbox-theme-adapta

  openSuseAdaptaTheme="PLACEHOLDER atm"

  ## This is what is called from the AdaptaInstall Function, below 

  ## I say this is needed since different branches of OpenSuse need different steps to install this theme, atm 


  ## Tumbleweed 
  installApp $openSuseAdaptaTheme

}

##############################################################################

## This seems to be a meta-function 
function adaptaThemeMetaInstall()
{

    # Install the theme, icon pack if there is one but do not install "wallpaper packs" and stuff like that

    # Solus has this as their default theme in the current version so skip any elif for Solus  


    packageName="adapta-gtk-theme"

    if [ "$distroBase" == 'ubuntu' ]; then
      

      ## From PPA comments
      ## https://launchpad.net/~tista/+archive/ubuntu/adapta

      ### * Supported Xenial, Zesty, Artful and Bionic (Gtk+ 3.18.x or higher)
      #### While it may be in some default repos ... IIRC, this is the official PPA the dev lists, so this is why I am doing it this way. 

      sudo add-apt-repository ppa:tista/adapta
      sudo apt update
      clear

    elif [ "$distroBase" == 'opensuse' ]; then

      ## Call Function, Above 
      openSuseAdaptaAdd

    else
      echo "Adapta Themme Installer Function: FILLER TEXT"
      return 
    fi


    installApp $packageName

}
