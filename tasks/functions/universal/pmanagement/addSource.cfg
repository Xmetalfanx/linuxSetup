function universalAddRepo
{

    if [ "${distroBase}" == 'ubuntu' ]; then
      addrepo= sudo apt-add-repository

    elif [ "${distroBase}" == 'fedora' ]; then
      echo "Filler - I forgot the command"

    elif [ "${distroBase}" == 'opennsuse' ]; then
              echo "Filler - I forgot the command"

    else
       echo "FILLER TEXT FOR NOW ... "
    fi



  # Arch


  # Fedora
    sudo dnf config-manager --add-repo $1

 
  ## OpenSuse
  function suseAddRepoName
  {
        ## $1 is the Repo Name
        ## $2 is the Repo "URL"

        #Adding Repos in OpenSuse
        sudo zypper addrepo -f -n $1 $2
  }

  function zypperAddRepo
  {
      zypper ar -r %1

      # Refresh Repo
      zypper ref
  }

  ## Ubuntu
  function addPPA
  {
    sudo apt-add-repository -y $1 $2 $3 $4 $5 $6 $7 $8
  }

}