
function osreleaseInfo
{
  idlike=($(cat /etc/os-release | grep ID_LIKE=))
  id=($(cat /etc/os-release | grep ID=))
  versionid=($(cat /etc/os-release | grep VERSION_ID=))
  version=($(cat /etc/os-release | grep VERSION=))
  ubuntuName=($(cat /etc/os-release | grep UBUNTU_CODENAME=))

  echo "Tests below "
  echo -e $idlike
  echo -e $id 
  echo -e $versionid
  echo -e $version

  userPrompt
}

function idCheck
{
  echo -e "Entered idCheck function"

  if [ "${id}" == 'arch' ]; then
    distroBase="arch"

  elif [ "${id}" == 'fedora' ]; then
    distroBase="fedora"

  elif [ "${id}" == 'opennsuse' ]; then
    distroBase="suse"       

  elif [ "${id}" == 'ubuntu' ]; then
    distroBase="ubuntu"


  else
    echo "FILLER TEXT FOR NOW ... "
  fi


}


function mainDisplayDistroInfo 
{
  echo -e "The base of your distro, appears to be: " $id
  echo -e "Your version (this doesn't always display info), appears to be: " $versionid


}


################################################################
## Not really sure what this fuction does but i am keeping it - just in case, for now 
function stripName
{


			strippedName="${prettyname#"PRETTY_NAME="}"

      # remove quotations AND periods
      finalStrippedName=`echo "$strippedName" | sed 's/\"//g;s/\.//g' `

      echo "$finalStrippedName"

      userPrompt

}


## Test from a post on Github
### lsb_release -ds | sed 's/^\"//g;s/\"$//g'
