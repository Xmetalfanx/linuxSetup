postInstallsDir="$rootdir"/postInstalls
themeDir="$rootdir"/tasks/themes

Packages= . "$rootdir"/tasks/functions/f_packageManager.cfg
$Packages

## Variables
themeHome=~/.themes
iconHome=~/.icons

##############################################################################
function quitScript
{
    clear
    echo "Goodbye"
    exit
}


function invalidSection
{
    echo "invalid selection "
    sleep 2s
    x=1
}

function checkDistro
{
    osrelease="$(grep 'ID_LIKE' /etc/os-release)"

    distro2=${osrelease#"ID_LIKE="}


     if [ "${distro}" == 'ubuntu' ]; then
          distro=Ubuntu
     elif [ "${distro}" == 'fedora' ]; then
          distro=Fedora
     elif [ "${distro}" == 'opennsuse' ]; then
          distro=Opensuse
     elif [ "${distro}" == 'arch' ]; then
        distro=Arch
     else
        echo "Tarball download and installs coming soon"
    fi

}


function extractTar
{
   tar -zxvf  %1
}

function userPrompt
{

  read -p "Press [Enter] to continue "
}

function gitDL
{
    stripFileInfo

    git clone $1
    echo -e "File Variable is now ${file}"
    userPrompt


    cd $file

    pwd
    userPrompt


}

function stripFileInfo
{
    #This is in Testing mode

  # get a URL from the theme function
  url=$1

  url2=${url}

  clear
  # Display Test for the full URl
  #echo -e "\n\nFull URL is:\t $url2"

  filename=${url2##*/}

  # Display Test for Total file Extension
  #echo -e "\nTotal Filename is:\t $filename"

  # Filename by itself
  file=${filename%%.*}
  #echo -e "Filename by itself: \t $file"

  extonly=${filename#*.}

  # Strip Numbers
  extnonum=`echo "$extonly" | sed 's/[0-9]*//g' `

  # Strip Periods
  extfinal=`echo "$extnonum" | sed 's/\.//g' `

  ## Display Test for final output
  #echo -e "Final Extension for test loop:\t $extfinal"

}

#######################################
# To move to another .cfg file later

function arcThemeNative
{
    ## https://software.opensuse.org/download.html?project=home%3AHorst3180&package=arc-theme

    echo -e "Visit the Offical Github page at: https://github.com/horst3180/Arc-theme"

  # If Arch

  # If Fedora
      ## Nothing Offical for 25 yet

      # 24
        dnf config-manager --add-repo http://download.opensuse.org/repositories/home:Horst3180/Fedora_24/home:Horst3180.repo
        dnf install arc-theme



  # If OpenSuse

    ## Tumbleweed URL - http://software.opensuse.org/ymp/home:Horst3180/openSUSE_Tumbleweed/arc-theme.ymp

    ## Leap 42.1 URL -http://software.opensuse.org/ymp/home:Horst3180/openSUSE_Leap_42.1/arc-theme.ymp

    # Leap 42.2 URL - http://software.opensuse.org/ymp/home:Horst3180/openSUSE_Leap_42.2/arc-theme.ymp


  # If Ubuntu
  ## For 16.04
    sudo sh -c "echo 'deb http://download.opensuse.org/repositories/home:/Horst3180/xUbuntu_16.04/ /' > /etc/apt/sources.list.d/arc-theme.list"
    sudo apt-get update
    sudo apt-get install arc-theme

    ## For 16.04
      sudo sh -c "echo 'deb http://download.opensuse.org/repositories/home:/Horst3180/xUbuntu_14.04/ /' > /etc/apt/sources.list.d/arc-theme.list"
      sudo apt-get update
      sudo apt-get install arc-theme



}
