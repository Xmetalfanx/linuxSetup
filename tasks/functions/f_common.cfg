#############################################
## Path Variables
postInstallsDir="$rootdir"/postInstalls
themeDir="$rootdir"/tasks/themes
scriptsDir="$rootdir"/scripts
functionfolder="$rootdir"/tasks/functions

themeHome=~/.themes
iconHome=~/.icons

##############################################################################

usefulApps=(hexchat filezilla smplayer mpv clementine deluge gparted pidgin libreoffice)

##############################################################################


distroCheck= . $functionfolder/f_distroCheck.cfg
$distroCheck


function quitScript
{
    clear
    echo "Goodbye"
    exit
}


function invalidSection
{
    echo "invalid selection "
    sleep 2s
    x=1
}


function extractTar
{
   tar -zxvf  %1
}

function userPrompt
{
  read -p "Press [Enter] to continue "
}


# git Download function to download from git repos 
function gitDL
{
    stripFileInfo

    git clone $1
    echo -e "File Variable is now ${file}"
    userPrompt

    cd $file

    pwd
    userPrompt

}

function stripFileInfo
{
  # This is in Testing mode

  # get a URL from the theme function
  url=$1

  filename=${url##*/}

  # Filename by itself
  file=${filename%%.*}

  extonly=${filename#*.}

  # Strip Numbers and Periods
  extfinal=`echo "$extonly" | sed 's/[0-9]*//g;s/\.//g' `

}

######################################################
## Both of these functions should do the same thing

function capsFirst
{
    ## To capitize the first letter of a variable
    ## May remove the need for certain other functions

    # This WORKS

    capVar=$1

    echo "${capVar^}"

}

function capsSED
{
   # me Doesn't work
    echo "$echo "$1" | sed `s/.*/\u&/`"

}


## Putting this here ... for now anyway

### Setup Git on ubuntu based repos.  This may be a newer version then is in repos 
function setupGitUbuntu
{
  sudo add-apt-repository ppa:git-core/ppa


  ## Change the update lines, down the line
  sudo apt-get update
  installApp build-essential libssl-dev libcurl4-gnutls-dev libexpat1-dev gettext unzip git-all

}

function setupGit
{
    // Separating this since this can be used on any distro

      ## SETUP GIT
      # Ask and read in name from user
      git config --global user.name "$userName"

      # Ask and read in email from user
      git config --global user.email "$userEmail"

}



function testArray
{
  clear
  count=1
  for i in ${usefulApps[@]}
  do

    echo -e "Hello"
    echo -e "count: $count"
    count=$[$count +1]
  done

  echo -e "Loop Done"
}
