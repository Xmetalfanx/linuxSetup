# needed info from INSIDE the function 
- PPAName 
- PPASource/PPAURL 
- packageName(s?) = think multiple options can be ok here ... 

## ?? use a meta-function here?

# Step 1 - Call a (coded yet? ... AddPPA function) that adds the PPA and maybe displays a "Adding $PPAName PPA" message to the user, with a user prompt after it so they can exit if they dont want PPAs 

# Step 2 - maybe redundent ... - refresh repo (sudo apt update)

# Step 3 - install the actual packages in packageName 

#################################

function thirdPartyRepo
{

    ###########################
    # $1 - the PPA Name  
    # $2 - PPA Source 
    # $3 - packageNames 
    ##########################


    ## Only Pass two arguements .. no need to pass $3 



    ## For Fedora, OpenSuse, Apt-Add-repo
    universalThirdPartyAdditionalRepos $1 $2 

    ## Here the repo is added 

}



function ppaInstall
{


}