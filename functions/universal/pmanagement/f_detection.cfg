function setAlreadyInstalled {

	grepPackage="grep $individualPackage"
	
	case $distroBase in

		arch) pacman -Q | $grepPackage ;; 
		debian|ubuntu) apt list --installed | $grepPackage ;;
		fedora) dnf list installed | $grepPackage ;;
		solus) eopkg li | $grepPackage ;;

	esac

	if [ "$?" == 0 ]; then
		alreadyInstalled="yes";
	elif [ "$?" == 1 ]; then
		alreadyInstalled="no"
	fi

}


# i dont like this idea but i am just putting code into practice here (vs just thinking of how to do it) ... i can refactor later
# considering changing this to detectPackageStatus and making what i have of that function name above say "detectPackageStatusThirdParty"
function detectPackageStatus() {

	for individualPackage in $@
	{
		clear

		echo -e "Checking $individualPackage is installed"

		# [ $? == 0 ] && echo -e "Package $individualPackage found on this system"  && userPrompt && return || echo -e "Package $individualPackage is not detected, and needs to be installed" || userPrompt || return $individualPackage

		#######################################################################
		# leaving here as a reminder to myself
		# echo -e "Error detecting package status "

		#########################################################################

		setAlreadyInstalled

		case "$alreadyInstalled" in

			no) 
				echo -e "$individualPackage is not installed"
				userPrompt
				$install $individualPackage
			 ;;

			yes) echo -e "$individualPackage is installed " ;;

		esac

	}
}


# function detectPackageStatusThirdParty()
# {
# 	for individualPackage in $@
# 	{
# 		clear

# 		echo -e "Checking $individualPackage is installed"

# 		# idea: so nothing is displayed
# 		which $individualPackage > /dev/null 2>&1

# 		# debugging
# 		# show exit code
# 		#echo $?

# 		if [ $? == 0 ]; then
# 			echo -e "Package $individualPackage found on this system"
# 			packageInstalled="yes"
# 			return
# 		else
# 			echo -e "Package $individualPackage is not detected, and needs to be installed"
# 			packageInstalled="no"
# 			#debugging prompt only
# 			# userPrompt
# 		fi
# 	}
# }