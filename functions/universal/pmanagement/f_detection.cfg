function detectInstallStatus {

	# declare Array 
	declare -a appsToBeInstalled


    for individualPackage in "$@"
    do
		if [[ $(command -v ${individualPackage} ) ]]; then 
			alreadyInstalled="yes";
		elif [[ ! $(command -v ${individualPackage} ) ]]; then
			alreadyInstalled="no"
			appsToBeInstalled+=("$individualPackage")
		fi

	done 
	#echo -e "${appsToBeInstalled[@]} are not installed"

}

# Meta "detect" function
#NOTHING should install here
function detectPackageStatus() {

	for individualPackage in $@
	{
		clear

		fancy_message action "Checking if $individualPackage is installed"

		###############################################
		# old cold kept here "just in case"
		# [ $? == 0 ] && fancy_message info "Package $individualPackage found on this system"  && userPrompt && return || fancy_message info "Package $individualPackage is not detected, and needs to be installed" || userPrompt || return $individualPackage
		#####################################################

		# function that uses the default package manager to detect if a package is installed and sets "$alreadyInstalled"
		detectInstallStatus "$individualPackage"

	}
}
