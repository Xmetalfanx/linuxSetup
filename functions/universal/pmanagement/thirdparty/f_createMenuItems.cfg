# many parts of this code were/are being removed 
function menuItemAddition()
{

    # customizeMenuFile is being removed but leave code below for now 
    customizeMenuFile "Icon" "$iconFile"    
    customizeMenuFile "Exec" "$execFile"
}
