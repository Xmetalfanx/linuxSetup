function optimizeDebianMirrors()
{
    clear
    echo -e "Installing netselect-apt"
    # apt-get needed? or would apt be ok?
    sudo apt-get install netselect-apt
}

function debianCodecs()
{
    #Install non-free codecs and other media components. ( Optional )

    # NOTE: These must be installed seperatly to avoid errors / conflicts.

    sudo apt-get install libav-tools 
    sudo apt-get install ttf-mscorefonts-installer
    sudo apt-get install icedtea-plugin
    sudo apt-get install libdvdcss2
    sudo apt-get install libdvd-pkg
    sudo dpkg-reconfigure libdvd-pkg

    sudo apt-get install browser-plugin-vlc
    sudo apt-get install libffmpegthumbnailer4
    sudo apt-get install ffmpegthumbnailer
    sudo apt-get install gstreamer1.0-plugins-bad
    sudo apt-get install gstreamer1.0-plugins-ugly
    sudo apt-get install gstreamer1.0-libav
    sudo apt-get install tumbler-plugins-extra


}

function debianLinuxFirmmware()
{
    sudo apt-get install gdebi gksu firmware-linux firmware-linux-nonfree

}

function debianIntelMicroCode()
{
    sudo apt-get install intel-microcode

}